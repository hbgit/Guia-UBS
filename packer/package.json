{
  "name": "@guia-ubs/packer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Constroi, valida, assina e publica os pacotes de conteudo",
  "scripts": {
    "build:pack": "tsx src/index.ts build",
    "publish:pack": "tsx src/index.ts publish",
    "test": "node --import tsx --test test/*.test.ts",
    "build:pack:dry": "tsx src/index.ts build --dry-run"
  },
  "dependencies": {
    "@guia-ubs/contract": "*",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
